---
- hosts: all
  gather_facts: true
  tasks:
    - name: run the setup facts
      ansible.builtin.setup:
    - name: run the windows setup facts
      ansible.windows.setup:
    - debug:
        msg: "{{ item }}"
      loop:
        - "{{ ansible_all_ipv4_addresses }}"
        - "{{ ansible_distribution }}"
        - "{{ ansible_distribution_version }}"
        - "{{ ansible_fqdn }}"
